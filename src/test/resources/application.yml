spring:
  application:
    name: jimmer-save
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:./test_db;database_to_upper=true

jimmer:
  dialect: org.babyfish.jimmer.sql.dialect.H2Dialect
  database-validation-mode: NONE
  trigger-type: binlog_only
  language: kotlin
